In mysql:
  Create db user: 'mvc' with password 'mvc'

  Run /migration script(s) to populate db with test values

  Make sure the following directories are writable by apache:
  /public/css
  /public/js
  /log
